import re

# re.findall(pattern, string, flags=0)
results = re.findall()

# search和findall
# search()方法可以返回匹配正则表达式的第一个内容
# 但是如果想要获取匹配正则表达式的所有内容，怎么办？
# 那就需要findall()这个方法了，
# 该方法会搜索整个字符串，然后返回匹配正则表达式的所有内容
