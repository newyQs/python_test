redis完全是开源的，遵守BSD协议，是一个高性能的key-value数据库。
redis与其他key-value缓存产品有以下三个特点:
1）redis支持数据的持久化，可以将内存中的数据保存在磁盘中，重启的时候可以再次加载进行使用；
2）redis不仅支持简单的key-value型的数据，还支持list,set,zset,hash等数据结构的存储；
3）redis支持数据的备份，即master-slave模式的数据备份；

redis的数据类型：
支持5种数据类型。分别为：string(字符串)，hash(哈希)，list（列表），set(集合)，zset(有序集合)